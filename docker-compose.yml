version: '3'

services:
  web:
    build: .
    ports:
      - '8080:8080'

  db:
    image: 'mongodb'
    environment:
      MONGODB_PASSWORD: 'mongodb'
    volumes:
      - db-data:/foo

volumes:
  db-data:
